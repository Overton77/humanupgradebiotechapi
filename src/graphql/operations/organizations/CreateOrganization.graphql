mutation CreateOrganization($data: OrganizationCreateInput!, $embedding: EmbeddingWriteOptionsInput) {
  createOrganization(data: $data, embedding: $embedding) {
    id
    slug
    name
    organizationType
  }
}
