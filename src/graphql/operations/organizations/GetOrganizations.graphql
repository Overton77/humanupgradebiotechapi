query GetOrganizations($input: OrganizationSearchInput) {
  organizations(input: $input) {
    total
    items {
      lexicalScore
      semanticScore
      hybridScore
      organization {
        id
        slug
        name
        organizationType
        tags
      }
    }
  }
}
