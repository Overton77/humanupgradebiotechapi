mutation CreateEpisode($data: EpisodeCreateInput!, $embedding: EmbeddingWriteOptionsInput) {
  createEpisode(data: $data, embedding: $embedding) {
    id
    slug
    title
    episodeNumber
    channelName
    publishedAt
    isPublished
  }
}
