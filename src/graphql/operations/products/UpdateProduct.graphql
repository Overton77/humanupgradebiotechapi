mutation UpdateProduct($where: ProductWhereUniqueInput!, $data: ProductUpdateInput!, $embedding: EmbeddingWriteOptionsInput) {
  updateProduct(where: $where, data: $data, embedding: $embedding) {
    id
    slug
    name
    category
    isActive
    tags
    categories
    benefits
  }
}
