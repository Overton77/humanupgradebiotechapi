query GetCompound($id: ID, $slug: String) {
  compound(id: $id, slug: $slug) {
    id
    slug
    name
    description
    canonicalName
    externalRef
    aliases
    mechanisms
    createdAt
    updatedAt
  }
}
