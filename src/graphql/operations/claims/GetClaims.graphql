query GetClaims($episodeId: ID, $limit: Int, $offset: Int) {
  claims(episodeId: $episodeId, limit: $limit, offset: $offset) {
    id
    episodeId
    speakerId
    text
    claimType
    stance
    claimConfidence
    tags
  }
}
